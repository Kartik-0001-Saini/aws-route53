# --- Web framework -------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Data layer ----------------------------------------------------------
SQLAlchemy==2.0.36
alembic==1.14.0

# --- Validation / settings -----------------------------------------------
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0

# --- Auth ----------------------------------------------------------------
# firebase-admin verifies Google-issued ID tokens against Google's public keys.
firebase-admin==6.6.0
# Signs the short-lived demo-user token, so the demo path uses the same
# Bearer-token contract as the real one instead of a special case.
PyJWT==2.10.1

# --- Testing -------------------------------------------------------------
pytest==8.3.4
httpx==0.28.1
